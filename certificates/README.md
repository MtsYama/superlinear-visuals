# Certificates（证书视觉资产）

结业证书相关的**视觉资产**归档位：底版图、坐标定义、脱敏展示样例。

## 规划结构

```
certificates/
├── backgrounds/   # 证书母版底版（导出的静态图）
├── coords/        # 文字叠加坐标定义（course-lang.json）
└── samples/       # 脱敏样例（展示用，非真实发出的证书）
```

- 只放视觉资产；发放流程、脚本、登记记录**不在本库**（内部系统维护）。
- 真实学员证书不入库。

> 状态：占位。
